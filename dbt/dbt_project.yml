name: "job_market"
version: "1.0.0"
profile: "job_market"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets: ["target", "dbt_packages"]

models:
  job_market:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
      fct_posting_snapshots:
        +materialized: incremental
    marts:
      +materialized: table
